#include <stdio.h>

int main() {
    scanf("%d",&num1);
    scanf("%d",&num2);
    if (num1) > (num2) {
        printf("True")
    }
    else {
        printf("False")
    }
    printf("%s", welcome());
    return 0;
}