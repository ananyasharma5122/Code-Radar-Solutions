// Your code here...
#include <stdio.h>

int main() {
    int num1, num2;

    // Input two numbers
    scanf("%d %d", &num1, &num2);

    // Check condition
    if (num1 >= num2 && num1%num2==0) {
        printf("Yes");

    } else if {
        printf("No");
    }

    return 0;
}
